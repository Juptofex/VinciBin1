<section class="biopage">
    <div class="container">
        <div class="content">
            <h1>Le Coach : </h1>
            <div class="coach-info">
                <div class="image-container">
                    <img src="{{coach.picture_path}}" {{coach.firstname}} {{coach.surname}}>
                </div>
                <div class="details">
                    <p>{{coach.firstname}} {{coach.surname}}</p>
                    <p>{{coach.email}}</p>
                    <p>{{coach.biography}}</p>
                </div>
            </div>
            <div class="update-btn-container">
                {{#if concordance}}
                    <a href="/coaches/update/{{idCoach}}" class="update-btn">Mettre à jour</a>
                {{/if}}
            </div>
            {{#if (eq session.user.status "normal")}}
    <div class="contact-form">
        <h3>Envoyer un message au coach</h3>
        <form action="/messages/sendToCoach/{{idCoach}}" method="post"> 
            <textarea name="message" placeholder="Message" required></textarea>
            <button type="submit" class="submit-btn">Envoyer</button>
        </form>
    </div>
{{/if}}
        </div>
    </div>
</section>